// to print numbers using loop
    #include<stdio.h>
    
    int main(){
        int i;
        for(i=0;i<=10;i++)
        {
            printf("%d\n",i);
            
        }
           printf("\n i value at the end : %d",i);
        return 0;

        
    }